package com.practice.interfacedemo.Demo1;

import java.util.ArrayList;

public interface Shape{

     int calculateArea();//by default, it is in public
    //we can also have the default method and static method in the interface.
    int age=21;//by default, the datamemebers and variables in interface
    //is in static and final
    //we cannot do any changes over it



}